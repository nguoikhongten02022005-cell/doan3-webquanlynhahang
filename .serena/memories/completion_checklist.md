# Completion Checklist

Before claiming completion, run fresh verification relevant to the change. For broad changes use `npm run validate`; for frontend-only checks use at least `npm run lint:frontend` and `npm run build:frontend`, plus browser validation for UI flows.

For this project, customer UI is expected on frontend port 5173 unless the user explicitly approves another port.
