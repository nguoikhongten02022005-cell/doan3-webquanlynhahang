# Style and Conventions

Frontend files use JSX with ES modules, functional React components, hooks, and Ant Design components. Naming in the frontend is mostly Vietnamese domain terminology. Existing code favors concise components and direct handlers rather than extra abstractions.

No dedicated frontend test framework is configured; frontend `npm run test` currently prints that no tests are configured.
